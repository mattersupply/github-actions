import { flags } from '@oclif/command';
import { BaseCommand } from '../../command';
import { RemoteConfigurationEntry } from '../../remote-config';
export declare class SetCommand extends BaseCommand {
    static description: string;
    static examples: string[];
    static flags: {
        entry: flags.IOptionFlag<string[]>;
        stage: flags.IOptionFlag<string[]>;
        config: flags.IOptionFlag<string>;
    };
    static args: import("@oclif/parser/lib/args").IArg<string>[];
    run(): Promise<void>;
    setConfigValues(stages: string[], entries: RemoteConfigurationEntry[]): Promise<void>;
}
