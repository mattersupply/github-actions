import { flags } from '@oclif/command';
import { BaseCommand } from '../../command';
import * as SSM from 'aws-sdk/clients/ssm';
export declare class DescribeCommand extends BaseCommand {
    static description: string;
    static examples: string[];
    static flags: {
        format: flags.IOptionFlag<string>;
        stage: flags.IOptionFlag<string[]>;
        config: flags.IOptionFlag<string>;
    };
    static args: import("@oclif/parser/lib/args").IArg<string>[];
    run(): Promise<void>;
    fetchValues(stage: string): Promise<import("aws-sdk/lib/request").PromiseResult<SSM.GetParametersByPathResult, import("aws-sdk").AWSError>>;
    printDescriptionAsDotenv(values: {
        [key: string]: SSM.Parameter;
    }): void;
    descriptionsByKey(parameters: SSM.ParameterList, stage: string): {
        [key: string]: SSM.Parameter;
    };
    mergeDescriptions(descriptions: {
        [key: string]: SSM.GetParametersByPathResult;
    }): any;
}
