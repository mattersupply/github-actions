"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const SSM = require("aws-sdk/clients/ssm");
const AWS = require("aws-sdk");
function createSSMConfigManager(config) {
    const credentials = new AWS.SharedIniFileCredentials({ profile: config === null || config === void 0 ? void 0 : config.get('aws.profile') });
    if (credentials.accessKeyId) {
        AWS.config.credentials = credentials;
    }
    return new SSM({ region: config === null || config === void 0 ? void 0 : config.get('aws.region') });
}
exports.createSSMConfigManager = createSSMConfigManager;
